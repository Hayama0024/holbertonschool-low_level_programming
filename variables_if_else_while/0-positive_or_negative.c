#include <stdio.h>
/**
 * main-Entry point
 *
 * return: Always 0 (Suces)
 */
int main(void)
{
	int n;

	if (n > 0)
	{
		printf("is positive\n");
	}
        else
	{
		if (n == 0)
		{
			printf("is zero\n");
		}
        		else
			{	
				printf("is negative\n");
			}
        return (0);
	}
}
